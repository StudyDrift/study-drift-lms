# 2.8 — Per-Attempt Question Shuffling & Answer Shuffling

> Implementation plan. Source: [docs/MISSING_FEATURES.md](../MISSING_FEATURES.md) §2.8.

## Metadata

| Field | Value |
|---|---|
| **Feature ID** | 2.8 |
| **Section** | Assessment & Authoring |
| **Severity** | MINOR |
| **Markets** | HE, K12 |
| **Status (today)** | MISSING |
| **Estimated effort** | XS (≤ 1 d) |
| **Owner (proposed)** | Backend team |
| **Depends on** | 2.1 (Question Bank) |
| **Unblocks** | None directly |

---

## 1. Problem Statement

Without question and answer shuffling, students sitting near each other can trivially share answers ("Question 3, answer B") because every student sees the same question order and the same option ordering. This is one of the most basic academic integrity measures in any quiz platform. The absence of shuffling in the `delivery` settings means Lextures cannot credibly serve in-person or online exams in K-12 or HE contexts where even rudimentary cheating deterrence is expected.

## 2. Goals

- Support per-attempt question shuffling (randomise question order for each attempt).
- Support per-attempt answer-option shuffling (randomise option order for MC and multi-select questions).
- Make both toggleable per quiz and per question (some questions, like ordered lists, should not have their options shuffled).
- Persist the shuffled order server-side so it is stable on page reload.

## 3. Non-Goals

- Shuffling for question types where order is semantically meaningful (ordering/sequence questions, matching questions — option shuffle disabled for these types automatically).
- True anti-cheating exam security (covered by 2.10 lockdown mode, 14.9 proctoring).
- Group-level shuffle seeds (same shuffle for all students in a section) — individual randomisation only.

## 4. Personas & User Stories

- **As an instructor**, I want to enable "shuffle questions" so each student gets a different ordering of the same 20 questions, making copying harder.
- **As an instructor**, I want to enable "shuffle answers" so the correct option appears in a different position for each student's attempt.
- **As a student**, I want the question and option order to remain stable if I reload the page mid-attempt, so I do not lose my place.
- **As an instructor**, I want to disable answer shuffling on specific questions (e.g., questions where "All of the above" or "None of the above" is the last option and should stay last).

## 5. Functional Requirements

- **FR-1.** The system MUST add `shuffle_questions` (boolean, default false) and `shuffle_answers` (boolean, default false) to the quiz delivery configuration (`module_quizzes`).
- **FR-2.** The system MUST add `shuffle_answers` (boolean, default true when parent quiz setting is true) per question in the question bank, allowing per-question override.
- **FR-3.** The system MUST compute the shuffled order at attempt start and persist it in `attempt_question_selections` (question order column) and a new `attempt_option_orders` table (per-question option permutation).
- **FR-4.** The system MUST serve questions and options in the persisted shuffled order for every subsequent request within the same attempt.
- **FR-5.** The system MUST NOT shuffle option order for question types `ordering`, `matching`, `hotspot`, or any type where option order is semantically determined.
- **FR-6.** The system MUST store the canonical correct answer in database-independent form (by `option_id`, not by position index) so grading is unaffected by shuffling.

## 6. Non-Functional Requirements

- **Performance** — Shuffle computation at attempt start is in-memory (Fischer-Yates O(n)); no additional DB queries. Persisting option order adds at most one INSERT per question in the attempt.
- **Security** — Shuffled order is generated server-side with a cryptographically random seed (`rand::thread_rng()` in Rust). Clients cannot observe or predict the order before attempt start.
- **Privacy & Compliance** — Shuffled order is an operational detail; standard attempt-record privacy applies (FERPA).
- **Accessibility** — Screen reader users MUST receive questions and options in a logical order even when shuffled; the visual order and DOM order MUST match the persisted shuffled order (no CSS reordering tricks).
- **Scalability** — No scaling concern; shuffle is O(n) per attempt.
- **Reliability** — Shuffle persisted atomically with attempt-start transaction; page reload always returns same order.
- **Observability** — Log shuffle settings applied per attempt at DEBUG level. No special metrics needed.
- **Maintainability** — Shuffle logic in `server/src/services/quiz_attempt.rs` as a pure function (`fn shuffle_questions(questions, seed) -> Vec<QuestionId>`).
- **Internationalization** — No i18n implications; shuffle is order-based.
- **Backward compatibility** — Default is `shuffle_questions = false, shuffle_answers = false`; existing quizzes unchanged.

## 7. Acceptance Criteria

- **AC-1.** *Given* a quiz with `shuffle_questions = true`, *when* two students start the quiz, *then* at least 90 % of the time their question orders differ (probabilistic; test with 50 attempt pairs).
- **AC-2.** *Given* a student reloads the quiz page mid-attempt, *when* the questions load, *then* the order is identical to before the reload.
- **AC-3.** *Given* a quiz with `shuffle_answers = true`, *when* two students start the quiz, *then* at least one MC question has options in different order for the two students.
- **AC-4.** *Given* a question has `shuffle_answers = false` override, *when* answers are shuffled for the quiz, *then* that specific question's options appear in authored order.
- **AC-5.** *Given* an ordering-type question is in a shuffled quiz, *when* the student sees the question, *then* the ordering-question's draggable items appear in the authored order (not shuffled).
- **AC-6.** *Given* options are shuffled and a student selects the correct answer, *when* the quiz is graded, *then* the grade is correct regardless of the option's shuffled position.

## 8. Data Model

```sql
-- Migration: server/migrations/0046_shuffling.sql

ALTER TABLE course.module_quizzes
    ADD COLUMN shuffle_questions BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN shuffle_answers   BOOLEAN NOT NULL DEFAULT FALSE;

ALTER TABLE course.questions
    ADD COLUMN shuffle_answers_override BOOLEAN;  -- NULL = inherit quiz setting

-- Persist per-attempt option order
CREATE TABLE course.attempt_option_orders (
    attempt_id  UUID NOT NULL,
    question_id UUID NOT NULL,
    option_order UUID[] NOT NULL,   -- ordered array of option_ids
    PRIMARY KEY (attempt_id, question_id)
);

-- attempt_question_selections.position already exists; ordering in that column now reflects shuffle
```

## 9. API Surface

No new endpoints. Shuffled question order reflected in the `position` of `attempt_question_selections`; option order reflected in how question options are returned in the `GET /api/v1/quiz-attempts/{id}/questions` response (options array ordered per `attempt_option_orders`).

```typescript
// Question response (per attempt) includes options in shuffled order:
interface DeliveredQuestion {
  question_id: string;
  version_number: number;
  position: number;       // shuffled position within attempt
  stem: string;
  options: AnswerOption[]; // ordered per attempt_option_orders
}
```

## 10. UI / UX

No new UI components needed. The quiz-take UI (`clients/web/src/pages/lms/CourseModuleQuizPage.tsx`) already renders questions and options in the order returned by the API; shuffling is transparent to the UI.

**Quiz settings panel** (instructor):
- Two new toggles: "Shuffle question order" and "Shuffle answer options."
- Question-level override: small "lock answer order" checkbox per question in the question list.

Accessibility: DOM order reflects visual order; no CSS `order` property used (which can break AT reading order).

## 11. AI / ML Considerations

Not applicable.

## 12. Integration Points

- `server/src/services/quiz_attempt.rs` — shuffle function called at attempt start.
- `server/migrations/0046_shuffling.sql`.
- `clients/web/src/pages/lms/CourseModuleQuizPage.tsx` — no changes needed; order is API-driven.
- Related plan: `02-assessment-and-authoring/2.1-question-bank-item-pool.md` — pool sampling + shuffling combined at attempt start.

## 13. Dependencies & Sequencing

- Must ship after: **2.1** (attempt_question_selections table).
- Very low risk; can be shipped early in Section 2.
- Shared infra: none.

## 14. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Shuffle doesn't persist → student sees different order on reload | L | H | Persist to `attempt_option_orders` atomically with attempt start |
| "All of the above" option shuffled to non-last position | M | M | Allow per-option `pin_last` flag; auto-detect "all of the above" patterns (v2) |
| Very small question pool makes collisions likely | L | L | Expected — document that shuffle becomes less effective with < 5 questions |

## 15. Rollout Plan

- **Feature flag:** not needed (settings default to false, no behaviour change for existing quizzes).
- **Migration sequencing:** add columns → deploy → instructors opt-in per quiz.
- **Pilot cohort:** any quiz instructor; no special cohort needed.
- **GA criteria:** shuffle persists across reload; grading unaffected by option order.
- **Rollback:** set shuffle columns to false for all quizzes; behaviour reverts to authored order.

## 16. Test Plan

- **Unit** — Fischer-Yates shuffle with seeded RNG; grade computation with shuffled option IDs.
- **Integration** — Start attempt with shuffle_questions=true → reload → assert same order; grade with shuffled options → assert correct grade.
- **End-to-end** — Playwright: two students start same shuffled quiz, compare question positions.
- **Security** — Shuffle seed generated server-side; no client parameter accepted.
- **Accessibility** — Verify DOM order matches visual order in shuffled quiz.
- **Performance** — Shuffle + persist for 50-question quiz adds ≤ 5 ms to attempt start.
- **Manual exploratory** — Verify ordering-type question not shuffled; verify "lock answer order" per-question override.

## 17. Documentation & Training

- Instructor guide: "Enabling question and answer shuffling for academic integrity."

## 18. Open Questions

1. Should the shuffle seed be stored explicitly (for reproducibility and audit) or is the persisted order sufficient?
2. Should instructors be able to view the shuffled order a specific student received?
3. Is there a use case for "same shuffle per cohort group" (e.g., all students in a section get the same random seed) for group assessments?

## 19. References

- Existing files: `server/src/routes/courses.rs`, `clients/web/src/pages/lms/CourseModuleQuizPage.tsx`, `server/migrations/`.
- External standards: Fischer-Yates shuffle (Knuth 1969); WCAG 2.1 SC 1.3.2 (Meaningful Sequence).
- Related plans: `02-assessment-and-authoring/2.1-question-bank-item-pool.md`, `02-assessment-and-authoring/2.10-lockdown-kiosk-mode.md`.
