package blueprintsync

// $1 = destination structure_item_id, $2 = source structure_item_id (blueprint)

const copyAssignmentSQL = `
INSERT INTO course.module_assignments (
	structure_item_id,
	markdown,
	points_worth,
	available_from,
	available_until,
	assignment_access_code,
	submission_allow_text,
	submission_allow_file_upload,
	submission_allow_url,
	late_submission_policy,
	late_penalty_percent,
	rubric_json,
	blind_grading,
	identities_revealed_at,
	moderated_grading,
	moderation_threshold_pct,
	moderator_user_id,
	provisional_grader_user_ids,
	originality_detection,
	originality_student_visibility,
	grading_type,
	posting_policy,
	release_at,
	never_drop,
	replace_with_final,
	settings_version,
	updated_at
)
SELECT
	$1::uuid,
	m.markdown,
	m.points_worth,
	m.available_from,
	m.available_until,
	m.assignment_access_code,
	m.submission_allow_text,
	m.submission_allow_file_upload,
	m.submission_allow_url,
	m.late_submission_policy,
	m.late_penalty_percent,
	m.rubric_json,
	m.blind_grading,
	m.identities_revealed_at,
	m.moderated_grading,
	m.moderation_threshold_pct,
	m.moderator_user_id,
	m.provisional_grader_user_ids,
	m.originality_detection,
	m.originality_student_visibility,
	m.grading_type,
	m.posting_policy,
	m.release_at,
	m.never_drop,
	m.replace_with_final,
	m.settings_version,
	NOW()
FROM course.module_assignments m WHERE m.structure_item_id = $2
`

const copyQuizSQL = `
INSERT INTO course.module_quizzes (
	structure_item_id,
	markdown,
	questions_json,
	available_from,
	available_until,
	unlimited_attempts,
	one_question_at_a_time,
	max_attempts,
	grade_attempt_policy,
	passing_score_percent,
	points_worth,
	late_submission_policy,
	late_penalty_percent,
	time_limit_minutes,
	timer_pause_when_tab_hidden,
	per_question_time_limit_seconds,
	show_score_timing,
	review_visibility,
	review_when,
	shuffle_questions,
	shuffle_choices,
	allow_back_navigation,
	quiz_access_code,
	adaptive_difficulty,
	adaptive_topic_balance,
	adaptive_stop_rule,
	random_question_pool_count,
	is_adaptive,
	adaptive_system_prompt,
	adaptive_source_item_ids,
	adaptive_question_count,
	adaptive_delivery_mode,
	lockdown_mode,
	focus_loss_threshold,
	never_drop,
	replace_with_final,
	settings_version,
	updated_at
)
SELECT
	$1::uuid,
	m.markdown,
	m.questions_json,
	m.available_from,
	m.available_until,
	m.unlimited_attempts,
	m.one_question_at_a_time,
	m.max_attempts,
	m.grade_attempt_policy,
	m.passing_score_percent,
	m.points_worth,
	m.late_submission_policy,
	m.late_penalty_percent,
	m.time_limit_minutes,
	m.timer_pause_when_tab_hidden,
	m.per_question_time_limit_seconds,
	m.show_score_timing,
	m.review_visibility,
	m.review_when,
	m.shuffle_questions,
	m.shuffle_choices,
	m.allow_back_navigation,
	m.quiz_access_code,
	m.adaptive_difficulty,
	m.adaptive_topic_balance,
	m.adaptive_stop_rule,
	m.random_question_pool_count,
	m.is_adaptive,
	m.adaptive_system_prompt,
	m.adaptive_source_item_ids,
	m.adaptive_question_count,
	m.adaptive_delivery_mode,
	m.lockdown_mode,
	m.focus_loss_threshold,
	m.never_drop,
	m.replace_with_final,
	m.settings_version,
	NOW()
FROM course.module_quizzes m WHERE m.structure_item_id = $2
`
