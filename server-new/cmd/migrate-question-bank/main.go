// Command migrate-question-bank is a placeholder for server/src/bin/migrate_question_bank.rs.
// When the question_bank repo and migration logic are ported, implement the same behavior here.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "migrate-question-bank: not implemented in Go; use the Rust binary in server/ until the port is complete.")
	os.Exit(1)
}
