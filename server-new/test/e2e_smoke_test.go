// Package test holds optional end-to-end style checks. HTTP parity tests from server/tests/
// are ported here incrementally.
package test

import "testing"

func TestPlaceholderE2E(t *testing.T) {
	// Staging E2E (migration.md §8) is run with the real API; this package reserves the path.
}
